// Twitter card image — re-uses the OpenGraph image so we maintain one
// source of truth for the share card visual.
export {
  default,
  runtime,
  alt,
  size,
  contentType,
} from "./opengraph-image";
